export enum UserStates {
  NOT_DISTURB = "not-disturb",
  AVAILABLE = "available",
  BUSY = "busy",
}

export enum UserStateColors {
  "not-disturb" = "danger",
  "available" = "success",
  "busy" = "warning",
}

export enum UserStateNames {
  "not-disturb" = "Do not Disturb",
  "available" = "Available",
  "busy" = "Busy",
}
